package com.xworkz.client.controller;
/*
@Controller
@RequestMapping("/user")
public class UserInfoController {

    @Autowired
    private UserInfoService userInfoService;

    @GetMapping("/showForm")
    public String showForm(Model model) {
        model.addAttribute("userInfo", new UserInfoDTO());
        return "user-form";
    }

    @PostMapping("/processForm")
    public String processForm(@ModelAttribute("userInfo") UserInfoDTO userInfoDTO) {
        userInfoService.saveUserInfo(userInfoDTO);
        return "redirect:/user/showForm"; // Redirect to the form again or to a success page
    }
}
*/